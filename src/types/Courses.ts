export interface Courses {
  id: number;
  title: string;
  color: string;
  taskCount: number;
}

export interface newCourse {
  title: string;
  color: string;
}

export interface Course {
  id: number;
  title: string;
  color: string;
}
